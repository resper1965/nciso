{
  "mcpServers": {
    "nciso-supabase": {
      "command": "node",
      "args": ["src/mcp/supabase-server.js"],
      "env": {
        "SUPABASE_URL": "${SUPABASE_URL}",
        "SUPABASE_ANON_KEY": "${SUPABASE_ANON_KEY}"
      }
    }
  }
} 